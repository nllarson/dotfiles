# enter your common aliases here.
# example : "alias junko='use grails 1.2.3; cd /path/to/whatever/junko/is;'"

# put aliases that you don't want in the git repo in the ~/.local_alias file
if [ -f  ~/.local_alias ]; then
    . ~/.local_alias
fi
